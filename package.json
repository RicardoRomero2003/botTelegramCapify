{
  "name": "capify-telegram-bot",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --keep-vars --secrets-file .env",
    "typecheck": "tsc --noEmit",
    "smoke": "tsx src/smoke.ts",
    "smoke:auth": "tsx src/smoke-auth.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.3",
    "wrangler": "^4.86.0"
  },
  "engines": {
    "node": "22.x"
  }
}
